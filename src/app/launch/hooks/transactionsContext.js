import { createContext } from 'react';

export const SetTransactionContext = createContext(null);
//export const TransactionsContext = createContext(null);
//export const GetTransactionContext = createContext(null);
//export const CheckTransactionsContext = createContext(null);
//export const HasPendingTransContext = createContext(null);

